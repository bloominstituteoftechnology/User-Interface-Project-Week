
// eventlistener.onscroll
// Greensock from (opacity:0 Y:opacity arriba)



  // let titles = Array.from(d.querySelectorAll('h2'))
  // console.log(titles[0]);
  //
  //
  //
	// // init controller
	// var controller = new ScrollMagic.Controller();
  //
  //
	// 	// build scene
	// 	var scene = new ScrollMagic.Scene({
	// 						triggerElement: titles[0]
	// 					})
	// 					.setTween(titles[0], 0.5, {backgroundColor: "green", scale: 2.5}) // trigger a TweenMax.to tween
	// 					// .addIndicators({name: "1 (duration: 0)"}) // add indicators (requires plugin)
	// 					.addTo(controller);
